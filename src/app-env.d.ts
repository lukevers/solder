/**
 * Build-time constants injected by Vite.
 *
 * Keeping these declarations in one place gives the client bundle typed access
 * to values resolved during `vite serve` and `vite build`.
 */
declare const __APP_VERSION__: string;
