/**
 * Standard audio sample rate used throughout
 * the app.
 *
 * Used by Web Audio API playback, waveform
 * display rendering, and audio-convert.ts when
 * resampling SPICE output to audio.
 */
export const SAMPLE_RATE = 44100;
