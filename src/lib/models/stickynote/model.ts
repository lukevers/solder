/**
 * Sticky-note symbol-domain model stub.
 *
 * Sticky notes are documentation-only UI elements
 * and never participate in netlist generation.
 *
 * There are no `.model` parameters to document for
 * this domain because it has no SPICE footprint.
 *
 * @see Ngspice User's Manual, `.MODEL`
 *   overview:
 *   https://ngspice.sourceforge.io/docs/ngspice-manual.pdf
 */
export {};
