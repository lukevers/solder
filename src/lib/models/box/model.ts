/**
 * Box symbol-domain model stub.
 *
 * Boxes are layout/grouping helpers for the editor and never emit SPICE
 * elements or model cards.
 *
 * There are no `.model` parameters to document for this domain because boxes
 * never participate in netlist generation.
 */
export {};
