/**
 * Capacitor symbol-domain model stub.
 *
 * Ideal capacitors are emitted directly as `C...` element lines in the netlist
 * compiler, so this domain does not need a separate `.model` card.
 *
 * There are no `.model` parameters to document for this domain in this repo.
 * Capacitor values are carried on the element instance line.
 *
 * @see https://ngspice.sourceforge.io/docs/ngspice-manual.pdf
 */
export {};
