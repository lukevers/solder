/**
 * Label symbol-domain model stub.
 *
 * Net labels merge otherwise-unwired ports into a shared SPICE node name and do
 * not correspond to a device model.
 *
 * There are no `.model` parameters to document for this domain because labels
 * are compile-time net aliases, not SPICE devices.
 *
 * @see https://ngspice.sourceforge.io/docs/ngspice-manual.pdf
 */
export {};
