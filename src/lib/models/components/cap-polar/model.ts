/**
 * Polarized-capacitor symbol-domain model stub.
 *
 * This domain reuses the ideal capacitor behavior from the netlist compiler and
 * does not need a dedicated `.model` card.
 *
 * There are no `.model` parameters to document for this domain in this repo.
 * Polarized capacitors are emitted as ideal capacitor elements with instance
 * values on the element line.
 *
 * @see https://ngspice.sourceforge.io/docs/ngspice-manual.pdf
 */
export {};
