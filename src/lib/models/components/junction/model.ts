/**
 * Junction symbol-domain model stub.
 *
 * Junction nodes are wiring artifacts that merge multiple handles into one
 * electrical net. They do not emit device elements or `.model` cards.
 *
 * There are no `.model` parameters to document for this domain because it only
 * affects connectivity.
 *
 * @see https://ngspice.sourceforge.io/docs/ngspice-manual.pdf
 */
export {};
