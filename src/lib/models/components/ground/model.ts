/**
 * Ground-symbol domain model stub.
 *
 * Ground nodes map directly to SPICE net `0`, so this domain does not emit a
 * device model.
 *
 * There are no `.model` parameters to document for this domain because it maps
 * straight to the simulator's global reference node.
 *
 * @see https://ngspice.sourceforge.io/docs/ngspice-manual.pdf
 */
export {};
