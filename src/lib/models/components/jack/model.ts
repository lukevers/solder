/**
 * Jack symbol-domain model stub.
 *
 * Input and output jacks define the external ports that the compiler wires to
 * `Vin` and `Rprobe`. They do not correspond to standalone `.model` cards.
 *
 * There are no local `.model` parameters to document for this domain. Jacks
 * participate in netlist generation through the compiler's use of independent
 * voltage sources and the output probe.
 *
 * @see https://ngspice.sourceforge.io/docs/ngspice-manual.pdf
 */
export {};
