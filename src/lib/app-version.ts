/**
 * App version label shown in the editor chrome.
 *
 * Vite injects this constant at build time. In dev it is the stable literal
 * `dev`; in production builds it resolves to the current short git hash.
 */
export const APP_VERSION = __APP_VERSION__;
